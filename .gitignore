node_modules/
.next/
out/
.env
.env.local
data/output/
*.log
.DS_Store

# artefactos de build
*.tsbuildinfo

# Vista materializada de la semilla + el log. Se reconstruye sola; el log es lo que va en git.
data/vocabulary/*.sqlite
data/vocabulary/*.sqlite-*

# Histórico de evaluación (SPEC-010): artefacto local de cada máquina, no una decisión versionada.
data/eval/*.sqlite*

# Histórico de MTOs procesados desde la app: datos reales de uso, no algo que versionar.
data/processing/*.sqlite*

# Eventos de KPI de uso (correcciones, vocabulario, compras): telemetría local de cada máquina.
# Mismo criterio que los dos de arriba — se quedaba fuera por olvido, no por decisión.
data/kpi/*.sqlite*
